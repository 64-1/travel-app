/** Minimum curated options per time-slot block after generation. */
export const SUGGESTIONS_PER_BLOCK_MIN = 5;

/** Maximum curated options per time-slot block after generation. */
export const SUGGESTIONS_PER_BLOCK_MAX = 7;
