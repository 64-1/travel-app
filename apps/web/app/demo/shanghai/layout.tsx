import { DemoTripLayout } from "@/components/DemoTripLayout";

export default function ShanghaiDemoLayout({ children }: { children: React.ReactNode }) {
  return <DemoTripLayout slug="shanghai">{children}</DemoTripLayout>;
}
